import { AuthorizedInterceptor } from './authorized.interceptor';

describe('AuthorizedInterceptor', () => {
  // it('should create an instance', () => {
  //   expect(new AuthorizedInterceptor()).toBeTruthy();
  // });
});
