import { ListDirective } from './list.directive';

describe('ListDirective', () => {
  it('should create an instance', () => {
    const directive = new ListDirective(null);
    expect(directive).toBeTruthy();
  });
});
