import { SelectDirective } from './select.directive';

describe('SelectDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
